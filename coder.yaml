coder:
  workspace:
    git:
      pre_commit:
        enable: false
    size: 3Gi
    dind:
      enable: true
      ram: 2Gi
  vscode:
    enable: true

    #
    # VSCode Extensions be to installed
    #
    extensions:
      - id: redhat.vscode-yaml          # Schema Validation in VSCode

