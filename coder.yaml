coder:
  workspace:
    image: hectorm/qemu-reactos:latest
    git:
      pre_commit:
        enable: false
    size: 2Gi
    dind:
      enable: true
      memory: 2Gi
      cpu: 1
  vscode:
    enable: false

    #
    # VSCode Extensions be to installed
    #
    extensions:
      - id: redhat.vscode-yaml          # Schema Validation in VSCode

